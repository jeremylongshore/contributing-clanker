# ===========================================
# Intentional Bounty - LangChain Orchestrator
# Vertex AI Agent Engine Deployment
# Last Updated: 2026-01-23
# ===========================================

# Core requirement for Agent Engine deployment
google-cloud-aiplatform[agent_engines,langchain,reasoningengine]>=1.112.0

# Core LangGraph (L1) - v1.0 is production stable
langgraph>=1.0.0,<2.0.0

# LangChain (L1) - MUST be 1.0+ for stable API
langchain>=1.0.0,<2.0.0
langchain-core>=0.3.0

# Vertex AI Integration (L2) - MUST be 3.2.1+ for Agent Engine telemetry
langchain-google-vertexai>=3.2.1

# PostgreSQL Checkpointing (L5) - NOT MemorySaver in prod
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0

# ===========================================
# CRITICAL: Agent Engine Serialization
# These MUST be pinned exactly - mismatch = HTTP 500
# ===========================================
cloudpickle==3.0.0        # MANDATORY: Serialization engine version
pydantic==2.7.4           # MANDATORY: Avoid v1/v2 FieldInfo errors

# A2A Communication with Bob's Brain
httpx>=0.25.0

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Google Cloud Platform
google-auth>=2.23.0
google-cloud-logging>=3.8.0

# Observability
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-exporter-gcp-trace>=1.5.0

# Development & Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Code Quality
black>=24.0.0
ruff>=0.1.0
mypy>=1.6.0
