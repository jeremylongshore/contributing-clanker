# Cloned repos (each has its own git)
appsmith/
cal-com/
calcom/
cortex/
feishin/
filament/
gumroad/
posthog/
projectdiscovery/
screenpipe/
shadcn-ui/
tldraw/
zio/

# Python
.coverage
__pycache__/
*.pyc

# Claude
.claude/

# Beads — track issues.jsonl + config for off-machine recovery; exclude
# the embedded Dolt DB (binary) and runtime state. The JSONL is the
# durable source of truth; on a fresh clone, bd reads it directly.
.beads/*
!.beads/issues.jsonl
!.beads/config.yaml

# Environment files with secrets
.env
.env.local
.env.production
.env.*.local
tools/.env
bounty-system/**/.env
bounty-system/**/.env.local
bounty-system/**/.env.production

# Sensitive documentation
postgresql-setup-manual.md
postgresql-setup-manual.pdf
valiant-ai-engineer-cover-letter.md
valiant-ai-engineer-cover-letter.pdf

# Additional cloned repos
vertex-ai-samples/
zio-blocks/
claude-cookbooks/

# Dolt database files (added by bd init)
.dolt/
*.db
